#include "window.h"
//#include "Q_learning.h"

//using namespace Win;
//using namespace QL;

int main()
{
	Win::win wi;

	//vector<int> arr = { 3,13,1,6 };
	/*list<int> arr = { 15,11,13,99,03 };/// for each
	for (auto gg : arr)
		cout << gg << endl;*/
	/*cout << arr.size() << endl;
	arr.size()--;

	cout << arr.size() << endl;*/
	system("pause");
	return 0;
}